// This program assumes that the input csv file will have a header
public class Main {
    public static void main(String[] args) {
        ApplicationWrapper application = new ApplicationWrapper();
        application.startApplication();
    }
}